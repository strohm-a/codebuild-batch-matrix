version: 0.2

phases:
  build:
    commands:
      - printenv
      - echo 'Running matrix1 build'
      - echo "GLOBAL_VAR = $GLOBAL_VAR" 
      - echo "STATIC_VAR = $STATIC_VAR" 
      - echo "MY_VAR = $MY_VAR" 
env:
  variables:
    MATRIX1_VAR: "MATRIX1"