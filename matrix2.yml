version: 0.2

phases:
  build:
    commands:
      - printenv
      - echo 'Running matrix2 build'
      - echo "GLOBAL_VAR = $GLOBAL_VAR" 
      - echo "STATIC_VAR = $STATIC_VAR" 
      - echo "MY_VAR = $MY_VAR" 
env:
  variables:
    MATRIX2_VAR: "MATRIX2"